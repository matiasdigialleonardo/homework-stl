#include <iostream>
#include <vector>

int main() {

    std::vector<int> integerVector = {5, 10, 15, 20};

    // Adds two elements at the end of the vector
    integerVector.push_back(25);
    integerVector.push_back(30);
    
    return 0;
}
